services:
  cnvlens:
    build: .
    image: cnvlens:local
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost/cnvlens/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
