services:
  lungseq:
    build: .
    image: lungseq-analyzer:local
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost/lungseq/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
